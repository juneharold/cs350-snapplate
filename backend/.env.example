# Postgres
DB_USER=snapplate
DB_PASSWORD=snapplate
DB_HOST=localhost
DB_PORT=5432
DB_NAME=snapplate

# Object storage (MinIO locally)
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=localdev
S3_SECRET_KEY=localdev123
S3_BUCKET=media
S3_REGION=us-east-1

# Kakao Local REST
KAKAO_REST_API_KEY=

# Auth (self-issued JWT) — must be >=32 bytes for HS256.
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-me-to-a-64-char-hex-secret-0000000000000000000000000000
JWT_EXPIRATION_HOURS=720

# Email (magic links)
SMTP_URL=
